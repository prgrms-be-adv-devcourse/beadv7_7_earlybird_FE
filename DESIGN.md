# EarlyBird 브랜드 디자인 원칙

> 새로운 아이디어를 누구보다 먼저 발견하고 함께 성장시키는 감성 크라우드펀딩 플랫폼 EarlyBird.
> 주 고객: 20~40대 IT・디자인・라이프스타일 얼리어답터, 새 프로젝트를 후원하는 사용자.

## 1. 컬러

| 역할 | 값 | 이유 |
| --- | --- | --- |
| 주조색 (Primary) | 탠저린 오렌지 `#FF7A45` | 이른 아침 해뜰 무렵의 온기를 담은 색이면서, Wadiz(민트)·Kickstarter(그린)·Indiegogo(핑크)가 아직 점유하지 않은 영역이라 브랜드가 바로 구분됨. |
| 배경색 | 크림 페이퍼 `#F2EFE6` | 경쟁사 특유의 차가운 순백 대신 손에 잡히는 종이 질감을 줘서 "감성" 포지셔닝을 배경에서부터 드러냄. |
| 본문 글자색 | 잉크 브라운블랙 `#2B2418` | 순검정 대신 따뜻한 톤을 섞어 눈의 피로를 줄이고, 팔레트 전체의 온기를 텍스트까지 일관되게 이어감. |

## 2. 폰트

| 역할 | 글꼴 | 이유 |
| --- | --- | --- |
| 제목용 | Cafe24 Ssurround | 둥근 형태라 다정하고 귀여운 인상을 주면서도 국문 글리프 완성도가 높아 헤드라인·강조 숫자에 바로 쓸 수 있음. |
| 본문용 | Pretendard | 국문 렌더링 품질과 굵기 폭이 넓어 긴 설명·라벨을 읽기 편하고, 제목 폰트의 개성을 방해하지 않는 중립적 파트너. |

## 3. 절대 쓰지 말아야 할 시각 요소

1. **초록·민트 계열 색상** — Wadiz(민트)와 Kickstarter(그린)가 "펀딩 성공 = 초록"이라는 공식을 이미 선점하고 있어, 어떤 초록 톤을 쓰든 둘 중 하나가 연상됨.
2. **스톡 일러스트・그라데이션 3D 아이소메트릭 이미지** — 스타트업 랜딩페이지의 몰개성적 기본값이라 "누구보다 먼저 발견한다"는 브랜드 정체성과 반대로 읽힘. 귀여움은 손그림 느낌의 단색 라인 아이콘·마스코트로 표현.
3. **"마감임박!!", "단 3개 남음" 식의 소리지르는 긴급 마케팅 배지·과장 카피** — 얼리어답터 타깃은 압박형 문구에 피로감을 느끼고, "함께 성장시키는" 다정한 브랜드 톤과도 어긋남.

## 4. 예외: 마스코트 시그니처 연출

브랜드 마스코트(새 캐릭터, `public/mascot-owl.jpg`)를 중심으로 한 은유 체계를 도입함: **리워드 = 애벌레(먹이), 후원자 = 새, 프로젝트 = 둥지, 모집 금액(펀딩률) = 알**. "EarlyBird"라는 이름 자체를 UI 경험으로 옮긴 핵심 브랜드 장치라, 3번 규칙(과시형 일러스트 배제)과 짧은 모션의 절제 원칙에 대해 **마스코트 관련 요소에 한해 예외**를 둔다.

- **정적 배지**(잉크 테두리 원형 프레임, `Mascot` 컴포넌트)는 로고 자리에 반복 사용 가능하다 — 헤더, 홈페이지 히어로/푸터, 로그인·회원가입 브랜드 패널에 적용됨. 원본 JPEG가 검정 배경이라 원형 크롭 + 확대로 모서리를 가리는 방식이며, 진짜 투명 배경(누끼) PNG를 구하면 그걸로 교체한다.
- 32px 안팎의 초소형 로고 자리(헤더 등)는 `Mascot`의 `variant="face"`(눈·부리만 보이도록 더 확대/크롭)를 쓴다 — 몸 전체가 나오는 기본 크롭은 이 크기에서 뭉개져 보임. 96px 이상 큰 배지(히어로/로그인 패널 코너)는 기본 `variant="full"`을 유지한다.
- **애니메이션(비행 연출)은 프로젝트 상세 페이지의 후원 패널 한 곳으로 계속 제한한다** — 정적 배지와 달리 반복 배치하지 않는다("보수는 한 곳에만 쓴다" 원칙은 모션에 한해 유지).
- 애니메이션은 1초 내외로 짧게 끝난다(원안의 "3~5초 비행"은 채택하지 않음).
- 펀딩률에 따른 알→부화→새끼 표시는 실제 데이터(달성률)를 그대로 반영하는 정직한 상태 표시이고, "새가 먹이를 물어다주는" 비행 연출은 실제 후원 액션이 아직 백엔드에 연동되지 않았으므로 **명시적으로 "미리보기(데모)"라고 라벨을 붙인 별도 트리거**로만 재생한다 — 비활성화된 실제 "후원하기" 버튼에 걸지 않는다.

## 참고: 기존 구현과의 충돌 (해결됨)

~~`tailwind.config.js`의 `mint` 토큰이 1번 규칙과 충돌~~ → `Badge`/`ProgressMeter`의 mint 사용을 `sun`(앰버) 톤으로 교체 완료. `mint`/`success` 토큰 자체도 tailwind config에서 제거함.
